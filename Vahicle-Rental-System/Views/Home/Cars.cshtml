@model IEnumerable<Vahicle_Rental_System.Models.Car>
@{
    ViewData["Title"] = "Choose Your Car";
    ViewData["ActivePage"] = "Cars";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="hero-wrap hero-wrap-2 js-fullheight" style="background-image: url('/images/bg_3.jpg');" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="container">
        <div class="row no-gutters slider-text js-fullheight align-items-end justify-content-start">
            <div class="col-md-9 ftco-animate pb-5">
                <p class="breadcrumbs">
                    <span class="mr-2"><a href="@Url.Action("Index", "Home")">Home <i class="ion-ios-arrow-forward"></i></a></span>
                    <span>Cars <i class="ion-ios-arrow-forward"></i></span>
                </p>
                <h1 class="mb-3 bread">Our Premium Fleet</h1>
            </div>
        </div>
    </div>
</section>

<section class="ftco-section bg-light">
    <div class="container">
        <div class="row">

            @if (!Model.Any())
            {
                <div class="col-12 text-center">
                    <h3 class="text-muted">No cars are currently available. Please check back later.</h3>
                </div>
            }
            else
            {
                @foreach (var car in Model)
                {
                    <div class="col-md-4">
                        <div class="car-wrap rounded ftco-animate mb-4 shadow-sm bg-white">
                            <div class="img rounded d-flex align-items-end" style="background-image: url('@car.ImageUrl'); height: 240px; background-size: cover;">
                            </div>
                            <div class="text p-4">
                                <h2 class="mb-0">
                                    <a href="@Url.Action("CarDetails", "Home", new { id = car.Id })">@car.Brand @car.Model</a>
                                </h2>
                                <div class="d-flex mb-3 align-items-center">
                                    <span class="cat text-muted">@(car.Category?.Name ?? "Standard")</span>
                                    <p class="price ml-auto mb-0" style="color: #01d28e; font-weight: 800;">
                                        PKR @car.PriceSelfDrive.ToString("N0") <span class="text-muted small">/day</span>
                                    </p>
                                </div>
                                <p class="d-flex mb-0">
                                    <a href="@Url.Action("CarDetails", "Home", new { id = car.Id })"
                                       class="btn btn-primary py-3 w-100 font-weight-bold"
                                       style="background-color: #01d28e; border: none; text-transform: uppercase; letter-spacing: 1px;">
                                        Book This Car
                                    </a>
                                </p>
                            </div>
                        </div>
                    </div>
                }
            }

        </div>

        <div class="row mt-5">
            <div class="col text-center">
                <div class="block-27">
                    <ul>
                        <li><a href="#">&lt;</a></li>
                        <li class="active"><span>1</span></li>
                        <li><a href="#">2</a></li>
                        <li><a href="#">3</a></li>
                        <li><a href="#">&gt;</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>